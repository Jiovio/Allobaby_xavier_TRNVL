import 'package:flutter/material.dart';

class Screeninglist extends StatelessWidget {
  const Screeninglist({super.key});

  

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}